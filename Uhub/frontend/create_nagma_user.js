-- Do not create UHub users from the browser console.
-- Provision accounts with User Management → Invite, then have the person
-- accept the invite and set their own password. Never put passwords in git.
console.warn(
  'create_nagma_user.js is retired. Use the invitation flow or an administrator account in public.users.'
);
